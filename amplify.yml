version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npx pnpm install
        build:
          commands:
            - npx pnpm --filter next-app build
      artifacts:
        baseDirectory: apps/next-app/.next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
      buildPath: /
    appRoot: apps/next-app
